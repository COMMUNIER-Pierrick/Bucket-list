{% extends 'base.html.twig' %}

{% block title %}

    Creation d'un nouveau souhait | {{ parent() }}

{% endblock %}

{% block body %}
    <div>
        {{ form_start(wishForm) }}

        {{ form_widget(wishForm) }}

        <button>Valider!</button>

        {{ form_end(wishForm) }}
    </div>

{% endblock %}