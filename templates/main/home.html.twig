{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Home{% endblock %}

{% block body %}
    <div class="hello">
    {% if app.user %}
    <h2>Hello {{ app.user.pseudo }}!</h2>
    {% else %}
    <h2>Hello !</h2>
    {% endif %}
    </div>
{% endblock %}
